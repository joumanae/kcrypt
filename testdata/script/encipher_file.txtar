# If a file path is specified, use that
exec encipher -k 1 message.txt
stdout 'IFMMP XPSME'
! stderr .

# Otherwise, read from standard input
stdin message.txt
exec encipher -k 1
stdout 'IFMMP XPSME'
! stderr .

-- message.txt --
hello world
